# Production Environment Variables Example
# 
# DO NOT create a .env.production file with real passwords!
# This file is just documentation of what variables are needed.
# 
# Instead, set these directly in AWS App Runner console when deploying.

# Database Configuration (set in App Runner console)
DATABASE_URL=postgresql://tally:YOUR_PASSWORD@splash-db.c10gmikqa1sm.us-east-2.rds.amazonaws.com:5432/splash

# Admin Credentials (set in App Runner console)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=YOUR_SECURE_ADMIN_PASSWORD

# Logging
LOG_LEVEL=info

# Note: RELOAD_FLAG should NOT be set in production

# SECURITY REMINDER:
# - Never commit files with real passwords
# - Set these values in AWS App Runner console only
# - This file is for documentation purposes only